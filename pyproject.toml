[build-system]
requires = ["hatchling>=1.31.0"]
build-backend = "hatchling.build"

[project]
name = "digital-twin-api"
version = "0.1.0"
description = "Cloud Run API for career chat (Vertex RAG + Gemini)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.1",
    "google-api-python-client>=2.198.0",
    "google-auth>=2.56.3",
    "google-cloud-error-reporting>=1.16.0",
    "google-cloud-logging>=3.16.2",
    "google-cloud-monitoring>=2.31.0",
    "google-cloud-storage>=3.13.1",
    "google-genai>=2.17.0",
    "google-cloud-aiplatform>=1.163.0",
    "google-cloud-bigquery>=3.43.0",
    "python-json-logger>=2.0.7",
    "sqlalchemy>=2.0.51",
    "alembic>=1.18.5",
    "psycopg[binary]>=3.3.4",
    "itsdangerous>=2.2.0",
    "google-auth-oauthlib>=1.2.0",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.28.0",
    "pytest>=9.1.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/digital_twin"]

[tool.hatch.build.targets.wheel.force-include]
"src/digital_twin/prompts/system.md" = "digital_twin/prompts/system.md"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff.lint]
ignore = ["E501"]
