# Local secrets — never commit
.env
.env.*
!.env.example
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
!terraform.tfvars.example
# Optional: partial backend overrides (bucket stays in committed backend.tf for CI).
terraform/backend.hcl
rag-sources/

# Terraform
.terraform/
# Commit .terraform.lock.hcl for reproducible provider versions.
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/

# Personal narrative / LinkedIn export — ingest via GCS + Vertex RAG, do not commit
src/digital_twin/prompts/knowledge.txt
src/digital_twin/prompts/knowledge.md
src/digital_twin/prompts/Profile.pdf

# OS / IDE
.DS_Store
.idea/
.vscode/
.cursor/
